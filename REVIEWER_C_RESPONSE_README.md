# Reviewer C – Comment Resolution Matrix (VoxAdapt)

**Purpose:** Track how each Reviewer C concern is addressed in the revision. This is formatted as a table so it can be copied into Excel later (one row = one atomic reviewer point).

**Legend (Status):** DONE = already revised/written or evidence exists; IN-PROGRESS = being executed; PLANNED = queued if reviewer requests more evidence.

| ID | Reviewer C point (extracted) | How we address it (paper + experiments) | Evidence / what we will cite | Where in manuscript (what changes) | Status |
|---|---|---|---|---|---|
| C1 | “Fully learnable voxel-scale optimization” claim is unsupported because rigorous gradient propagation through nondifferentiable discretization is not demonstrated. | **Clarify the actual differentiable path**: VoxAdapt learns (i) scale weights / assignments via Gumbel-Softmax relaxation and (ii) scale parameters used by the multi-scale feature construction; **voxel coordinate quantization remains discrete** in sparse-conv pipelines. Add an explicit “Differentiability & Gradient Path” paragraph and soften any wording that implies gradients flow through coordinate discretization. | (i) Code reality: scale assignment uses Gumbel-Softmax; (ii) limitation: integer voxel coords are required for sparse conv; (iii) add a short gradient-path schematic (loss → detector → fused features → scale-weights/scale-params). | Abstract + Method overview: replace “gradients flow to voxelization geometry” type claims with “differentiable relaxation of scale assignment and fusion; discretization remains discrete.” | DONE (abstract wording updated) + IN-PROGRESS (method clarification paragraph)
| C2 | Stability analysis for ScaleNet (Gumbel-Softmax) is overlooked; convergence in near-discrete assignment regime is theoretically unsupported; gradient variance instability concerns. | Add **empirical stability evidence** + training dynamics discussion: (1) learning curves showing early exploration vs later convergence, (2) report temperature schedule, (3) show that performance improves after annealing (warm-up needed), (4) note gradient clipping and any regularization used. Avoid over-claiming a full theoretical proof; present standard relaxation rationale + empirical behavior in LiDAR training. | From `PAPER_IMPROVEMENT_ACTION_PLAN.md`: 2-epoch underperformance (exploration) vs 5-epoch +2.89% (annealed, converging). From configs: gradient clipping is enabled in schedules. | Experiments / Training details: add a subsection “Stability of Gumbel-Softmax ScaleNet” with: schedule equation, qualitative explanation, and a short ablation plan if needed. | IN-PROGRESS
| C3 | Novelty of learnable voxel scales is ambiguous vs dynamic voxelization and DSVT-style adaptive discretization; insufficient algorithmic differentiation. | Add a **Related Work / Positioning** paragraph and a compact comparison table. Clearly distinguish: (a) VoxAdapt = learnable scale parameters + per-point scale weighting within multi-scale voxel feature construction for sparse-conv detectors; (b) dynamic voxelization/DSVT = different objectives/operators (e.g., tokenization/transformer processing, dynamic partitioning) and typically not framed as learning continuous voxel size parameters in SECOND-like sparse-conv voxel backbones. Also acknowledge shared theme (“adaptive discretization”) while specifying what is new in VoxAdapt (learned scale values + importance-guided assignment + lightweight ScaleNet + fusion). | Use existing repo docs as internal support (e.g., architecture overview and implementation description) plus citations in the paper. Keep claims precise: novelty is in *learnable scale values + assignment/fusion mechanism* under sparse-conv constraints. | Related Work + Method contributions bullet list: rewrite to explicitly contrast with dynamic voxelization / DSVT at an abstraction level (inputs/outputs/operator differences). | PLANNED (paper text)
| C4 | Effectiveness of weighted multi-scale fusion is unexamined; “feature interference” across parallel voxel grids in sparse convolution is ignored. | Add **fusion ablations** and explicit architectural explanation: why naive multi-scale can hurt (feature dilution/interference), and how VoxAdapt mitigates it (learned per-point weights, controlled fusion, importance-guided routing). Include an ablation: uniform averaging vs learned fusion (Gumbel-weighted) vs concatenation (if available). | From `PAPER_IMPROVEMENT_ACTION_PLAN.md`: fixed multi-scale baseline is worse than single-scale (e.g., -2.47% at 5 epochs), supporting interference/dilution claim. From `EXPERIMENTAL_RESULTS.md`: fixed multi-scale + Gumbel fusion achieves strong AP (shows weighted fusion can work). | Experiments/Ablations section: add “Fusion strategy” ablation table + short discussion. | IN-PROGRESS
| C5 | Generality claims tied to SECOND backbone are baseless; no evidence for transferability to PointPillars, PV-RCNN, or transformer-based detectors. | **Restrict claims** to what is actually evaluated (SECOND/SECFPN) and move broader generality to “future work” unless we can add at least one additional backbone experiment. If time permits, run one additional backbone baseline + VoxAdapt variant to demonstrate transfer. | Current evidence: only SECOND-based experiments. Planned evidence (optional): PointPillars or PV-RCNN integration experiment on KITTI (same protocol). | Abstract/Intro/Conclusion: remove or soften “generally applicable across detectors” language; add limitation statement + future work. | DONE (claim-softening started in abstract) + PLANNED (full consistency pass)
| C6 | “Naive multi-scale voxelization degrades performance” claim is inaccurate because baseline uses uniform averaging rather than established attention-driven / FPN-inspired fusion paradigms. | Clarify what “naive” means in this paper: uniform averaging / fixed equal weights across scales. Add an **established fusion baseline proxy**: learned fusion (our Gumbel-weighted fusion) and/or concatenation. Cite that attention/FPN-style fusion exists and position our fusion as a lightweight weighting mechanism suitable for sparse voxel features. | `EXPERIMENTAL_RESULTS.md` already documents fixed multi-scale with Gumbel-weighted fusion. Add/plan: uniform averaging baseline and concatenation baseline if not already run. | Baselines section: explicitly define “uniform averaging multi-scale” and update wording to avoid implying that all multi-scale fusion is bad. | IN-PROGRESS
| C7 | Interpretation of temperature annealing effects is questionable; interactions between scheduling and learning dynamics are poorly characterized in sparse LiDAR training regimes. | Add temperature schedule definition + rationale (exploration→exploitation), and correlate with observed learning curves. Optionally add a small sensitivity study: 2–3 schedules (fast vs slow decay; different τ0) to show robustness. | `PAPER_IMPROVEMENT_ACTION_PLAN.md` has a clear narrative: 2 epochs (high τ) underperforms; by 5 epochs (lower τ) it improves strongly. | Training details + Ablations: add a short “Temperature schedule” subsection and a figure/table. | IN-PROGRESS
| C8 | Robustness of detection-loss-driven optimization lacks depth; no regularization prevents collapse to identical discretization values. | Document the **anti-collapse regularization** that exists in the implementation (scale bounds + diversity term) and/or the diversity encouragement used in logits. Report the regularization weight(s) used in experiments and add an ablation (remove regularization) if needed. | Code evidence: `ImportanceGuidedMultiScaleVFE.get_scale_regularization_loss()` includes bound + diversity (variance) term; also includes an entropy-style diversity encouragement applied to logits. | Method / Loss function: add explicit formula + explain how it prevents scale collapse; optionally add “w/o regularization” ablation. | IN-PROGRESS
| C9 | Pedestrian gains attribution is vague; confounds (focal loss, imbalance handling, sampling bias) are not excluded. | Tighten causal claims: treat pedestrian improvement as **empirical observation**, not proof. Add controlled experiments: keep loss/sampling identical and only toggle adaptive voxelization. Optionally add: (i) fixed loss variants vs adaptive voxelization, (ii) per-class/size-stratified analysis. | Planned: multi-class experiments (Pedestrian, Cyclist) per `PAPER_IMPROVEMENT_ACTION_PLAN.md` plus controlled toggles. | Results discussion: soften causal language; add a short “Threats to validity / confounds” paragraph. | PLANNED
| C10 | Edge feasibility assessment is deficient; parallel grids + VFEs + fusion lacks formal complexity accounting under realistic constraints. | Add explicit compute + memory accounting: number of voxels processed per scale, FLOPs proxy (or wall-clock latency), peak GPU memory, throughput. Provide a clear comparison vs single-scale baseline and report “marginal overhead” with actual measured numbers. | Existing partial evidence: iteration time and memory notes in `EXPERIMENTAL_RESULTS.md`; compute planning notes in `PAPER_IMPROVEMENT_ACTION_PLAN.md`. Planned: a small table with latency/memory for 1-scale vs multi-scale vs VoxAdapt. | Experiments/Deployment section: add a “Complexity & Resource Cost” table (runtime, memory, params) and statement of target platform assumptions. | IN-PROGRESS

---

## Notes to self (for revision pass)

- Ensure the same differentiability/claim wording is consistent across **Abstract, Introduction, Method, and Conclusion**.
- Avoid saying “gradients flow through discretization/voxelization geometry”; instead say “gradients flow through the relaxed assignment/fusion and learned scale parameters; voxel coordinate quantization remains discrete in sparse voxel backbones.”
- When using the phrase “naive multi-scale,” explicitly define it as “uniform averaging / fixed equal weights across scales.”

## Sources in this repository (internal)

- `PAPER_IMPROVEMENT_ACTION_PLAN.md` (learning dynamics; 2-epoch vs 5-epoch results; annealing narrative; fixed multi-scale underperformance)
- `EXPERIMENTAL_RESULTS.md` (fixed multi-scale + Gumbel fusion results; timing/memory notes)
- `mmdet3d/models/voxel_encoders/importance_guided_multi_scale_vfe.py` (temperature schedule; regularization helper; diversity encouragement)
- `mmdet3d/models/voxel_encoders/fixed_multi_scale_vfe.py` (Gumbel fusion implementation; temperature decay)
